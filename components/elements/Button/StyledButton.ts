import styled from "styled-components"
import { TouchableOpacity } from "react-native"
import { Text } from "../../Themed"

export const CustomButtonStyled = styled(TouchableOpacity)`
    padding: 15px;
    border-radius: 10px;
    align-items: center;
`
export const CustomTextbutton = styled(Text)`
    font-weight: bold;
    font-size: 16px;
`
